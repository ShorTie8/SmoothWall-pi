include ../Makefile.conf

PACKAGE = lynx
VERSION = 2.9.2
EXTENSION = .tar.bz2
LICENCE_FILE = $(DIR)/COPYING
DIR = $(PACKAGE)$(VERSION)
TARBALL = $(PACKAGE)$(VERSION)
BASE_URL = https://invisible-mirror.net/archives/lynx/tarballs

CONFIG_OPTS += --with-screen=ncurses

include ../Makefile.rules
